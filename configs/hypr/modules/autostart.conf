#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = waybar
exec-once = awww-daemon & awww img ~/walls/wall.png --transition-type none

exec-once = rm -rf ~/.cache && rm -rf ~/.bash_history
